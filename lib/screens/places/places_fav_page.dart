import 'package:flutter/material.dart';

class PlacesFavPage extends StatelessWidget {
  const PlacesFavPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Заведения избранные'));
  }
}