import 'package:flutter/material.dart';

class PromotionsMyPage extends StatelessWidget {
  const PromotionsMyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Акции мои'));
  }
}