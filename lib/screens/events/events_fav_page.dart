import 'package:flutter/material.dart';

class EventsFavPage extends StatelessWidget {
  const EventsFavPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Мероприятия Избранные'));
  }
}