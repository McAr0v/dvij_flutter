import 'package:flutter/material.dart';

class EventsMyPage extends StatelessWidget {
  const EventsMyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Мероприятия мои'));
  }
}